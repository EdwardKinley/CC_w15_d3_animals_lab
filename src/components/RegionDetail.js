import React from 'react';
// import CountryContainer from '../containers/CountryContainer.js';

const RegionDetail = function (props) {
  return (
      <button>{props.region}</button>
  );
}

export default RegionDetail;
